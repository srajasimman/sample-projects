apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-springboot-gradle.fullname" . }}
  labels:
    {{- include "java-springboot-gradle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-springboot-gradle.selectorLabels" . | nindent 4 }}
