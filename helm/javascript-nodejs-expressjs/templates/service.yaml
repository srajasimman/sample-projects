apiVersion: v1
kind: Service
metadata:
  name: {{ include "javascript-nodejs-expressjs.fullname" . }}
  labels:
    {{- include "javascript-nodejs-expressjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javascript-nodejs-expressjs.selectorLabels" . | nindent 4 }}
