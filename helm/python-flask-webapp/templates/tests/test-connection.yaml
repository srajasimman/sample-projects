apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-flask-webapp.fullname" . }}-test-connection"
  labels:
    {{- include "python-flask-webapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-flask-webapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
