apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-flask-webapp.fullname" . }}
  labels:
    {{- include "python-flask-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-flask-webapp.selectorLabels" . | nindent 4 }}
