apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-springboot-maven.fullname" . }}-test-connection"
  labels:
    {{- include "java-springboot-maven.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-springboot-maven.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
