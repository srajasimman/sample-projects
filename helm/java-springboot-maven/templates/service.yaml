apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-springboot-maven.fullname" . }}
  labels:
    {{- include "java-springboot-maven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-springboot-maven.selectorLabels" . | nindent 4 }}
